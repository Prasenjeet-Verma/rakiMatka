<!doctype html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Milan Morning</title>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.29.4/moment.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/moment-timezone/0.5.43/moment-timezone-with-data.min.js"></script>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
  <link rel="stylesheet" href="/css/userMainGame.css" />
  <style type="text/tailwindcss">
    @layer components {
        /* Custom Digit Card Style */
        .digit-card {
          @apply relative basis-[30%] aspect-[4/3] bg-[#005c4b] text-white rounded-xl 
                       flex items-center justify-center text-3xl font-medium shadow-md 
                       cursor-pointer transition-all active:scale-95 select-none;
        }
        /* The small bet amount badge */
        .badge {
          @apply absolute top-1 right-2 sm:top-2 sm:right-3 text-[10px] sm:text-xs opacity-80 font-normal;
        }
      }
        .g-hide-scroll::-webkit-scrollbar { display: none; }
        .g-hide-scroll { -ms-overflow-style: none; scrollbar-width: none; }
        input[type=number]::-webkit-inner-spin-button, 
        input[type=number]::-webkit-outer-spin-button { -webkit-appearance: none; margin: 0; }
        
        .g-full-height {
            height: 100vh;
            height: -webkit-fill-available;
        }
        .tp-hidden {
            display: none !important;
        }

        .g-bg-teal { background-color: #005c4b; }
        .g-text-teal { color: #005c4b; }
        .g-border-teal { border-color: #005c4b; }
                /* Removes the up/down arrows from number inputs */
        input::-webkit-outer-spin-button,
        input::-webkit-inner-spin-button { -webkit-appearance: none; margin: 0; }
        input[type=number] { -moz-appearance: textfield; }
        .hidden { display: none; }
        #scroll-area::-webkit-scrollbar { display: none; }
        #scroll-area { -ms-overflow-style: none; scrollbar-width: none; }
        
        @keyframes slideIn {
            from { opacity: 0; transform: translateX(-10px); }
            to { opacity: 1; transform: translateX(0); }
        }
        .entry-anim { animation: slideIn 0.25s ease-out forwards; }
               /* Custom UI for Radio Markers */
        input[type="radio"]:checked + .radio-indicator {
            border-color: #005c4b;
        }
        input[type="radio"]:checked + .radio-indicator::after {
            content: "";
            display: block;
            width: 10px;
            height: 10px;
            background: #005c4b;
            border-radius: 50%;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }

    </style>
  <script>
    // Extending Tailwind to include your specific brand color
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            brandGreen: '#005c4b',
          }
        }
      }
    }
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            'primary-teal': '#005c4b',
            'surface-gray': '#f1f2f6',
            'item-green': '#e6edea'
          }
        }
      }
    }
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            'brand-teal': '#005c4b',
            'accent-green': '#7da15c',
            'app-bg': '#f0f2f5',
          }
        }
      }
    }
  </script>
  <!-- =================================== Body Start ======================================= -->
</head>

<body class="bg-gray-100">
  <!-- HEADER -->
  <header class="bg-teal-800 text-white px-4 py-5 rounded-b-3xl fixed w-[100%]">
    <div class="flex items-center gap-3">
      <a href="/userdashboard">
        <i class="fa-solid fa-arrow-left text-xl cursor-pointer"></i>
      </a>
      <h1 class="text-lg font-semibold">
        <%= game.gameName %>
      </h1>
    </div>
  </header>

  <!-- CONTENT -->
  <div class="p-4 grid grid-cols-2 gap-4 pt-[5.5rem]">
    <!-- CARD -->
    <div id="btnSingleDigit" class="bg-white rounded-2xl shadow-lg p-4 flex flex-col items-center cursor-pointer">
      <div class="w-24 h-24 bg-teal-800 rounded-full flex items-center justify-center shadow-md">
        <img src="/svg/dice.svg" class="w-12" />
      </div>
      <p class="mt-4 font-semibold text-gray-800">Single Digit</p>
    </div>


    <!-- CARD -->
    <div id="btnBulkDigit" class="bg-white rounded-2xl shadow-lg p-4 flex flex-col items-center cursor-pointer">
      <div class="w-24 h-24 bg-teal-800 rounded-full flex items-center justify-center shadow-md">
        <img src="/svg/dice.svg" class="w-12" />
      </div>
      <p class="mt-4 font-semibold text-gray-800">Single Bulk Digit</p>
    </div>


    <!-- CARD -->
    <div id="btnJodiDigit" class="bg-white rounded-2xl shadow-lg p-4 flex flex-col items-center">
      <div class="w-24 h-24 bg-teal-800 rounded-full flex items-center justify-center shadow-md">
        <img src="/svg/dices-dice-svgrepo-com.svg" class="w-12" />
      </div>
      <p class="mt-4 font-semibold text-gray-800">Jodi Digit</p>
    </div>

    <!-- CARD -->
    <div id="btnJodiDigitBulk" class="bg-white rounded-2xl shadow-lg p-4 flex flex-col items-center">
      <div class="w-24 h-24 bg-teal-800 rounded-full flex items-center justify-center shadow-md">
        <img src="/svg/dice.svg" class="w-12" />
      </div>
      <p class="mt-4 font-semibold text-gray-800">Jodi Digit Bulk</p>
    </div>

    <!-- CARD -->
    <div id="btnSinglePanna" class="bg-white rounded-2xl shadow-lg p-4 flex flex-col items-center">
      <div class="w-24 h-24 bg-teal-800 rounded-full flex items-center justify-center shadow-md">
        <img src="/svg/playing-card-clover-3-svgrepo-com.svg" class="w-12" />
      </div>
      <p class="mt-4 font-semibold text-gray-800">Single Panna</p>
    </div>

    <!-- CARD -->
    <div id="btnSinglePannaBulk" class="bg-white rounded-2xl shadow-lg p-4 flex flex-col items-center">
      <div class="w-24 h-24 bg-teal-800 rounded-full flex items-center justify-center shadow-md">
        <img src="/svg/playing-card-clover-3-svgrepo-com.svg" class="w-12" />
      </div>
      <p class="mt-4 font-semibold text-gray-800">Single Panna Bulk</p>
    </div>

    <!-- CARD -->
    <div id="btnDoublePanna" class="bg-white rounded-2xl shadow-lg p-4 flex flex-col items-center">
      <div class="w-24 h-24 bg-teal-800 rounded-full flex items-center justify-center shadow-md">
        <img src="/svg/poker-cards-svgrepo-com.svg" class="w-12" />
      </div>
      <p class="mt-4 font-semibold text-gray-800">Double Panna</p>
    </div>
    <!-- CARD -->
    <div id="btnDoublePannaBulk" class="bg-white rounded-2xl shadow-lg p-4 flex flex-col items-center">
      <div class="w-24 h-24 bg-teal-800 rounded-full flex items-center justify-center shadow-md">
        <img src="/svg/poker-cards-svgrepo-com.svg" class="w-12" />
      </div>
      <p class="mt-4 font-semibold text-gray-800">Double Panna Bulk</p>
    </div>
    <!-- CARD -->
    <div id="btnTriplePanna" class="bg-white rounded-2xl shadow-lg p-4 flex flex-col items-center">
      <div class="w-24 h-24 bg-teal-800 rounded-full flex items-center justify-center shadow-md">
        <img src="/svg/casino-cards-svgrepo-com.svg" class="w-12" />
      </div>
      <p class="mt-4 font-semibold text-gray-800">Triple Panna</p>
    </div>
    <!-- CARD -->
    <div id="btnOddEven" class="bg-white rounded-2xl shadow-lg p-4 flex flex-col items-center">
      <div class="w-24 h-24 bg-teal-800 rounded-full flex items-center justify-center shadow-md">
        <img src="/svg/dices-dice-svgrepo-com.svg" class="w-12" />
      </div>
      <p class="mt-4 font-semibold text-gray-800">Odd Even</p>
    </div>
    <!-- CARD -->
    <div id="btnHalfSangam" class="bg-white rounded-2xl shadow-lg p-4 flex flex-col items-center">
      <div class="w-24 h-24 bg-teal-800 rounded-full flex items-center justify-center shadow-md">
        <img src="/svg/poker-svgrepo-com.svg" class="w-12" />
      </div>
      <p class="mt-4 font-semibold text-gray-800">Half Sangam</p>
    </div>

    <!-- CARD -->
    <div id="btnFullSangam" class="bg-white rounded-2xl shadow-lg p-4 flex flex-col items-center">
      <div class="w-24 h-24 bg-teal-800 rounded-full flex items-center justify-center shadow-md">
        <img src="/svg/poker-svgrepo-com.svg" class="w-12" />
      </div>
      <p class="mt-4 font-semibold text-gray-800">Full Sangam</p>
    </div>

    <!-- CARD -->
    <div id="btnSPMotor" class="bg-white rounded-2xl shadow-lg p-4 flex flex-col items-center">
      <div class="w-24 h-24 bg-teal-800 rounded-full flex items-center justify-center shadow-md">
        <img src="/svg/casino-mobile-svgrepo-com.svg" class="w-12" />
      </div>
      <p class="mt-4 font-semibold text-gray-800">SP Motor</p>
    </div>

    <!-- CARD -->
    <div id="btnDPMotor" class="bg-white rounded-2xl shadow-lg p-4 flex flex-col items-center">
      <div class="w-24 h-24 bg-teal-800 rounded-full flex items-center justify-center shadow-md">
        <img src="/svg/craps-svgrepo-com.svg" class="w-12" />
      </div>
      <p class="mt-4 font-semibold text-gray-800">DP Motor</p>
    </div>

    <!-- CARD -->
    <div id="btnSPDPTP" class="bg-white rounded-2xl shadow-lg p-4 flex flex-col items-center">
      <div class="w-24 h-24 bg-teal-800 rounded-full flex items-center justify-center shadow-md">
        <img src="/svg/casino-chips-svgrepo-com.svg" class="w-12" />
      </div>
      <p class="mt-4 font-semibold text-gray-800">SP DP TP</p>
    </div>
    <!-- CARD -->
    <div id="btnRedBracket" class="bg-white rounded-2xl shadow-lg p-4 flex flex-col items-center cursor-pointer">
      <div class="w-24 h-24 bg-teal-800 rounded-full flex items-center justify-center shadow-md">
        <img src="/svg/main.svg" class="w-12" />
      </div>
      <p class="mt-4 font-semibold text-gray-800">Red Bracket</p>
    </div>
  </div>

  <!-- ===================================== Single Digit ================================ -->

  <main id="singleDigitGame" data-game-id="<%= game._id %>" data-game-name="<%= game.gameName %>"
    data-game-type="SINGLE_DIGIT" data-open-time="<%= todaySchedule ? todaySchedule.openTime : '' %>"
    data-close-time="<%= todaySchedule ? todaySchedule.closeTime : '' %>"
    class="bg-white min-h-screen flex flex-col hidden fixed top-0 w-full z-50">

    <!-- HEADER -->

    <header class="bg-teal-800 text-white px-4 py-5 rounded-b-3xl">
      <div class="flex items-center gap-3">
        <i id="closeSingle" class="fa-solid fa-arrow-left text-xl cursor-pointer"></i>
        <h1 class="text-lg font-semibold">Single Digit - <%= game.gameName %>
        </h1>
      </div>
    </header>

    <div class="h-[90vh] flex flex-col justify-between p-5 overflow-x-auto">

      <!-- OPEN / CLOSE -->
      <select id="betTypeSingle" class="border border-black rounded-full px-6 py-2 font-bold w-full text-center mb-6">
        <option value="open">OPEN</option>
        <option value="close">CLOSE</option>
      </select>

      <!-- INPUT GRID -->
      <div id="singleDigitGrid" class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-3 mb-8"></div>

      <!-- TOTAL -->
      <div class="flex mb-8">
        <div class="flex w-full border-2 border-[#005c4b] rounded-lg overflow-hidden">
          <div class="bg-[#005c4b] text-white px-4 py-3 flex-1 text-center font-bold">
            Total
          </div>
          <div id="singleDigitTotal" class="bg-white px-4 py-3 flex-1 text-center font-bold">
            0
          </div>
        </div>
      </div>

      <!-- SUBMIT -->
      <button id="submitSingleDigit" class="w-full bg-[#005c4b] text-white py-4 rounded-full text-lg font-bold">
        Submit
      </button>

    </div>
  </main>

  <!-- ===================================== Single Bulk Digit ================================ -->
  <main id="SingleBulkDigit" data-game-id="<%= game._id %>" data-game-name="<%= game.gameName %>"
    data-game-type="SINGLE_BULK_DIGIT" data-open-time="<%= todaySchedule ? todaySchedule.openTime : '' %>"
    data-close-time="<%= todaySchedule ? todaySchedule.closeTime : '' %>"
    class="bg-white min-h-screen flex flex-col hidden fixed top-0 w-full z-50">
    <!-- HEADER -->
    <header class="bg-teal-800 text-white px-4 py-5 rounded-b-3xl">
      <div class="flex items-center gap-3">
        <i id="closeBulk" class="fa-solid fa-arrow-left text-xl cursor-pointer"></i>
        <h1 class="text-lg font-semibold">Single Bulk Digit - <%= game.gameName %>
        </h1>
      </div>
    </header>

    <div class="px-5 pt-6 space-y-6 flex-1 overflow-x-scroll">
      <!-- SESSION -->
      <div class="flex justify-between items-center">
        <div class="text-xl font-bold">Choose<br />Session</div>
        <div class="flex space-x-6">
          <label class="flex items-center space-x-2">
            <input type="radio" name="sessionSB" value="OPEN" />
            <span class="font-bold">Open</span>
          </label>
          <label class="flex items-center space-x-2">
            <input type="radio" name="sessionSB" value="CLOSE" />
            <span class="font-bold">Close</span>
          </label>
        </div>
      </div>

      <!-- POINTS -->
      <div class="flex justify-between items-center">
        <span class="text-xl font-bold">Points</span>
        <input id="pointsInput" type="number" placeholder="Enter Point"
          class="w-48 py-3 px-4 rounded-xl text-center font-bold" />
      </div>

      <!-- GRID -->
      <div id="gridContainer" class="flex flex-wrap justify-center gap-3 w-full max-w-md mx-auto pb-10"></div>
    </div>

    <!-- FOOTER -->
    <div class="border-t bg-white py-4 sticky bottom-0">
      <div class="flex justify-between items-center px-6">
        <div>
          <div class="text-xs uppercase">Bids</div>
          <div id="totalBids" class="text-xl font-black">0</div>
        </div>
        <div>
          <div class="text-xs uppercase">Total Amount</div>
          <div id="totalAmount" class="text-xl font-black">0</div>
        </div>
        <button onclick="submitBidsofsinglebulk()" class="bg-[#065f56] text-white px-10 py-3 rounded-xl font-bold">
          Submit
        </button>
      </div>
    </div>
  </main>


  <!-- ===================================== Jodi Digit ================================ -->

  <main id="jodiDigit" data-game-id="<%= game._id %>" data-game-name="<%= game.gameName %>"
    data-open-time="<%= todaySchedule ? todaySchedule.openTime : '' %>"
    data-close-time="<%= todaySchedule ? todaySchedule.closeTime : '' %>"
    class="bg-white min-h-screen flex flex-col hidden fixed top-0 w-full">
    <header class="bg-teal-800 text-white px-4 py-5 rounded-b-3xl shadow-md">
      <div class="flex items-center gap-3">
        <i id="closeJodiDigit" class="fa-solid fa-arrow-left text-xl cursor-pointer"></i>
        <h1 class="text-lg font-semibold uppercase tracking-wide">Jodi Digit - <%= game.gameName %>
        </h1>
      </div>
    </header>

    <div class="flex-1 flex flex-col bg-gray-50 overflow-hidden">
      <nav id="jodiTabs" class="flex gap-3 p-4 overflow-x-auto no-scrollbar bg-white border-b border-gray-100 shrink-0">
      </nav>

      <div id="jodiGrid" class="flex-1 grid grid-cols-2 gap-4 p-4 overflow-y-auto content-start">
      </div>
    </div>

    <footer
      class="bg-white p-4 border-t border-gray-200 flex justify-between items-center shadow-[0_-4px_10px_rgba(0,0,0,0.05)] sticky bottom-0">
      <div class="flex flex-col">
        <span class="text-[10px] text-gray-400 uppercase font-black tracking-widest">Total Amount</span>
        <div class="flex items-center text-xl font-black text-[#005c4b]">
          <span class="mr-1">₹</span>
          <span id="jodiTotalAmount">0</span>
        </div>
      </div>
      <button id="submitJodiBtn" onclick="submitJodiBids()"
        class="bg-[#005c4b] hover:opacity-90 text-white px-10 py-3 rounded-xl font-bold transition-all active:scale-95 shadow-lg">
        Submit Bid
      </button>
    </footer>
  </main>


  <!-- ===================================== Jodi Digit Bulk ================================ -->
 <main id="jodiDigitBulk"
  data-game-id="<%= game._id %>"
  data-game-name="<%= game.gameName %>"
  data-open-time="<%= todaySchedule ? todaySchedule.openTime : '' %>"
  class="bg-white min-h-screen flex flex-col hidden fixed top-0 w-full">

  <!-- HEADER -->
  <header class="bg-teal-800 text-white px-4 py-5 rounded-b-3xl">
    <div class="flex items-center gap-3">
      <i id="closeJodiDigitBulk" class="fa-solid fa-arrow-left text-xl cursor-pointer"></i>
      <h1 class="text-lg font-semibold">Jodi Digit Bulk - <%= game.gameName %></h1>
    </div>
  </header>

  <div class="px-5 pt-8 space-y-6 flex-1">

    <!-- MAIN DIGIT -->
    <div class="flex justify-between items-center">
      <h2 class="text-xl font-bold">Open Digit</h2>
      <input
        id="jodiDigitBulk_opendigit"
        type="text"
        inputmode="numeric"
        placeholder="0-9"
        class="w-40 px-4 py-3 border rounded-xl font-bold disabled:bg-gray-200 disabled:text-gray-500 disabled:cursor-not-allowed"
      />
    </div>

    <!-- POINTS -->
    <div class="flex justify-between items-center">
      <h2 class="text-xl font-bold">Points</h2>
      <input id="jodiDigitBulk_points" type="number"
        class="w-40 px-4 py-3 border rounded-xl font-bold disabled:bg-gray-200 disabled:text-gray-500 disabled:cursor-not-allowed" />
    </div>

    <!-- ADD -->
    <div class="flex justify-end">
      <button id="jodiDigitBulk_addbtn"
        class="bg-teal-800 text-white px-14 py-3 rounded-xl font-bold disabled:opacity-50 disabled:cursor-not-allowed">
        Add
      </button>
    </div>

    <!-- TABLE -->
    <table class="w-full text-center border mt-4">
      <thead class="bg-gray-100 font-bold">
        <tr>
          <td>Digit</td>
          <td>Amount</td>
          <td>Action</td>
        </tr>
      </thead>
      <tbody id="jodiDigitBulkTable"></tbody>
    </table>

    <!-- SUBMIT -->
    <div class="flex justify-center pt-6">
      <button id="jodiDigitBulk_submitbtn"
        class="bg-teal-800 text-white px-24 py-4 rounded-xl font-bold disabled:opacity-50 disabled:cursor-not-allowed">
        Submit
      </button>
    </div>
  </div>

  <!-- BOTTOM -->
  <div class="border-t py-4">
    <div class="flex justify-around font-bold">
      <div>Bids <div class="jodiDigitBulk_totalbids">0</div></div>
      <div>Amount <div class="jodiDigitBulk_totalamount">0</div></div>
    </div>
  </div>
</main>

  <!-- ===================================== Single Panna ================================ -->
  <main id="singlePanna" data-game-id="<%= game._id %>" data-game-name="<%= game.gameName %>"
    data-open-time="<%= todaySchedule ? todaySchedule.openTime : '' %>"
    data-close-time="<%= todaySchedule ? todaySchedule.closeTime : '' %>"
    class="bg-white min-h-screen flex flex-col hidden fixed top-0 w-full">
    <!-- HEADER -->

    <header class="bg-teal-800 text-white px-4 py-5 rounded-b-3xl">
      <div class="flex items-center gap-3">
        <i id="closeSinglePanna" class="fa-solid fa-arrow-left text-xl cursor-pointer"></i>
        <h1 class="text-lg font-semibold">Single Panna - <%= game.gameName %>
        </h1>
      </div>
    </header>

    <div class="w-[100%] bg-white md:rounded-2xl shadow-2xl flex flex-col h-screen">
      <div class="bg-white border-b border-gray-100 shrink-0">
        <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center px-4 py-3 gap-3">
          <span class="text-sm font-semibold text-gray-500 uppercase tracking-wider">Select Game Type</span>
          <div class="relative w-full sm:w-auto">
            <select id="betType" name="betType" class="w-full sm:w-48 appearance-none h-11
         border-2 border-[#005c4b]
         rounded-full px-6 font-bold text-sm
         bg-white text-[#005c4b] cursor-pointer">
              <option value="open">OPEN</option>
              <option value="close">CLOSE</option>
            </select>

            <div class="absolute right-4 top-2.5 pointer-events-none text-[#005c4b]">▼</div>
          </div>
        </div>

        <div id="digitBar"
          class="flex gap-2 overflow-x-auto px-4 py-4 no-scrollbar bg-gray-50/50 border-t border-gray-100">
        </div>
      </div>

      <main class="flex-grow overflow-y-auto p-4 md:p-8 custom-scrollbar">
        <div id="pattiGrid" class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6 gap-4">
        </div>
      </main>

      <footer
        class="bg-white p-4 md:p-6 border-t border-gray-200 shadow-[0_-10px_30px_rgba(0,0,0,0.05)] shrink-0 relative bottom-20">
        <div class="max-w-4xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-4">
          <div class="flex items-baseline gap-2">
            <span class="text-gray-400 font-bold uppercase text-xs">Total Bid:</span>
            <span id="grandTotal" class="text-3xl font-black text-[#005c4b]">0</span>
          </div>
          <button onclick="submitBids()"
            class="w-full sm:w-64 bg-[#005c4b] text-white py-4 rounded-xl font-bold uppercase tracking-widest hover:bg-[#004a3c] active:scale-95 transition-all shadow-lg">
            Submit Bid
          </button>
        </div>
      </footer>
    </div>
    </div>

  </main>

  <!-- ===================================== Single Panna Bulk ================================ !-->
  <main id="singlePannaBulk" data-game-id="<%= game._id %>" data-game-name="<%= game.gameName %>"
    data-open-time="<%= todaySchedule ? todaySchedule.openTime : '' %>"
    data-close-time="<%= todaySchedule ? todaySchedule.closeTime : '' %>"
    class="bg-white min-h-screen flex flex-col hidden fixed top-0 w-full ">
    <!-- HEADER -->

    <header class="bg-teal-800 text-white px-4 py-5 rounded-b-3xl">
      <div class="flex items-center gap-3">
        <i id="closeSinglePannaBulk" class="fa-solid fa-arrow-left text-xl cursor-pointer"></i>
        <h1 class="text-lg font-semibold">Single Panna Bulk - <%= game.gameName %>
        </h1>
      </div>
    </header>
    <div class="bg-zinc-300 md:p-8 w-[100%]">

      <div
        class="bg-surface-gray min-h-screen md:min-h-[850px] flex flex-col relative shadow-2xl md:rounded-[2.5rem] overflow-hidden">


        <div class="bg-white p-5 space-y-4 border-b border-zinc-100">
          <div class="flex items-center justify-between">
            <label class="text-zinc-500 text-[10px] font-black uppercase tracking-wider">Session Type</label>
            <button id="toggle-session" onclick="handleToggle()"
              class="border-2 border-zinc-200 rounded-xl px-10 py-2 min-w-[150px] text-xs font-black text-zinc-700 hover:border-primary-teal transition-all">
              OPEN
            </button>
          </div>
          <div class="flex items-center justify-between">
            <label class="text-zinc-500 text-[10px] font-black uppercase tracking-wider">Point Value</label>
            <input type="number" id="val-input" value=""
              class="border-2 border-zinc-200 rounded-xl px-4 py-2 w-[150px] text-center text-sm font-bold focus:border-primary-teal focus:ring-0 outline-none transition-all">
          </div>
        </div>

        <div class="grid grid-cols-5 gap-3 p-5 bg-white">
          <button onclick="processSelection(0)"
            class="bg-[#005c4b] text-white py-3.5 rounded-2xl text-xl font-bold shadow-md hover:brightness-110 active:scale-90 transition-all">
            0 </button>
          <button onclick="processSelection(1)"
            class="bg-[#005c4b] text-white py-3.5 rounded-2xl text-xl font-bold shadow-md hover:brightness-110 active:scale-90 transition-all">
            1 </button>
          <button onclick="processSelection(2)"
            class="bg-[#005c4b] text-white py-3.5 rounded-2xl text-xl font-bold shadow-md hover:brightness-110 active:scale-90 transition-all">
            2 </button>
          <button onclick="processSelection(3)"
            class="bg-[#005c4b] text-white py-3.5 rounded-2xl text-xl font-bold shadow-md hover:brightness-110 active:scale-90 transition-all">
            3 </button>
          <button onclick="processSelection(4)"
            class="bg-[#005c4b] text-white py-3.5 rounded-2xl text-xl font-bold shadow-md hover:brightness-110 active:scale-90 transition-all">
            4 </button>
          <button onclick="processSelection(5)"
            class="bg-[#005c4b] text-white py-3.5 rounded-2xl text-xl font-bold shadow-md hover:brightness-110 active:scale-90 transition-all">
            5 </button>
          <button onclick="processSelection(6)"
            class="bg-[#005c4b] text-white py-3.5 rounded-2xl text-xl font-bold shadow-md hover:brightness-110 active:scale-90 transition-all">
            6 </button>
          <button onclick="processSelection(7)"
            class="bg-[#005c4b] text-white py-3.5 rounded-2xl text-xl font-bold shadow-md hover:brightness-110 active:scale-90 transition-all">
            7 </button>
          <button onclick="processSelection(8)"
            class="bg-[#005c4b] text-white py-3.5 rounded-2xl text-xl font-bold shadow-md hover:brightness-110 active:scale-90 transition-all">
            8 </button>
          <button onclick="processSelection(9)"
            class="bg-[#005c4b] text-white py-3.5 rounded-2xl text-xl font-bold shadow-md hover:brightness-110 active:scale-90 transition-all">
            9 </button>
        </div>

        <div style="height: 45vh; overflow-y: scroll;">
          <div
            class="grid grid-cols-4 px-8 py-3 text-[9px] font-black text-[#005c4b] uppercase tracking-[0.2em] text-center border-y border-zinc-100">
            <span>Digits</span>
            <span>Pts</span>
            <span>Mode</span>
            <span></span>
          </div>

          <div id="scroll-area" class="flex-1 overflow-y-auto pb-40 pt-3 space-y-2"></div>
        </div>

        <footer class="absolute bottom-0 w-full bg-white border-t border-zinc-200 p-5 
        shadow-[0_-10px_20px_rgba(0,0,0,0.03)] rounded-t-[2rem] bottom-[4rem]">
          <div class="flex justify-between items-center mb-5 px-2">
            <div class="text-left">
              <p class="text-[9px] text-zinc-400 font-black uppercase tracking-tighter">Items</p>
              <p id="stat-count" class="text-2xl font-black text-zinc-800">0</p>
            </div>
            <div class="text-right">
              <p class="text-[9px] text-zinc-400 font-black uppercase tracking-tighter">Total Credits</p>
              <p id="stat-sum" class="text-2xl font-black text-primary-teal">0</p>
            </div>
          </div>
          <button onclick="submitBulkBets()"
            class="w-full bg-[#005c4b] text-white py-4 rounded-2xl font-black text-xs uppercase tracking-[0.2em] shadow-lg shadow-teal-900/20 hover:scale-[1.02] active:scale-95 transition-all">
            Place Order
          </button>
        </footer>
      </div>
    </div>

  </main>


  <!-- ===================================== Double Panna ================================ -->
  <main id="DoublePanna" data-game-id="<%= game._id %>" data-game-name="<%= game.gameName %>"
    data-open-time="<%= todaySchedule ? todaySchedule.openTime : '' %>"
    data-close-time="<%= todaySchedule ? todaySchedule.closeTime : '' %>"
    class="bg-white min-h-screen flex flex-col hidden fixed top-0 w-full">
    <!-- HEADER -->

    <header class="bg-teal-800 text-white px-4 py-5 rounded-b-3xl">
      <div class="flex items-center gap-3">
        <i id="closeDoublePanna" class="fa-solid fa-arrow-left text-xl cursor-pointer"></i>
        <h1 class="text-lg font-semibold">Double Panna - <%= game.gameName %>
        </h1>
      </div>
    </header>


    <div class="bg-slate-200 font-sans antialiased overflow-hidden text-slate-900">

      <div class="flex items-center justify-center min-h-screen">

        <div class=" flex flex-col g-full-height bg-white shadow-2xl overflow-hidden md:rounded-2xl ">

          <section class="p-4 space-y-4 bg-white z-40 shadow-sm border-b w-[100%]">
            <div class="flex flex-col sm:flex-row justify-between items-center gap-4">
              <select id="g-game-selector" onchange="updateGameMode(this.value)"
                class="w-full sm:w-1/2 py-2.5 px-5 border border-slate-300 rounded-full font-bold text-xs bg-white focus:outline-none focus:border-[#005c4b] cursor-pointer hover:bg-slate-50 transition-all uppercase appearance-none text-center">
                <option value="OPEN">OPEN</option>
                <option value="CLOSE">CLOSE</option>
              </select>
            </div>

            <div id="g-tab-wrapper" class="flex flex-row justify-between gap-2 overflow-x-auto g-hide-scroll py-2">
            </div>
          </section>

          <main class="flex-1 overflow-y-auto p-4 g-hide-scroll bg-slate-50">
            <div id="g-main-grid" class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3 md:gap-5">
            </div>
          </main>

          <footer class="p-4 border-t bg-white shadow-[0_-5px_20px_rgba(0,0,0,0.06)] relative bottom-[4.5rem]">
            <div class="flex justify-between items-center mb-5 px-1">
              <span class="text-slate-400 font-bold uppercase text-[10px] md:text-xs tracking-widest">Total
                Amount</span>
              <span id="g-total-display"
                class="text-2xl md:text-4xl font-black g-text-teal tracking-tighter italic">0</span>
            </div>
            <button
              class="w-full g-bg-teal hover:bg-[#004a3d] text-white py-4.5 rounded-2xl font-black text-sm md:text-lg active:scale-[0.96] transition-all shadow-xl uppercase tracking-[0.2em] p-[0.8rem]">
              Submit Bid
            </button>
          </footer>
        </div>
      </div>
    </div>

  </main>

  <!-- ===================================== Double Panna Bulk ================================ -->
  <main id="DoublePannaBulk" data-game-id="<%= game._id %>" data-game-name="<%= game.gameName %>"
    data-open-time="<%= todaySchedule ? todaySchedule.openTime : '' %>"
    data-close-time="<%= todaySchedule ? todaySchedule.closeTime : '' %>"
    class="bg-white min-h-screen flex flex-col hidden fixed top-0 w-full">
    <!-- HEADER -->

    <header class="bg-teal-800 text-white px-4 py-5 rounded-b-3xl">
      <div class="flex items-center gap-3">
        <i id="closeDoublePannaBulk" class="fa-solid fa-arrow-left text-xl cursor-pointer"></i>
        <h1 class="text-lg font-semibold">Double Panna Bulk - <%= game.gameName %>
        </h1>
      </div>
    </header>

    <div class="bg-gray-200 flex justify-center items-center md:py-8 font-sans">

      <div
        class="w-full h-screen-safe md:h-[850px] flex flex-col shadow-2xl md:rounded-3xl overflow-hidden border border-gray-300">

        <main class="flex-1 p-4 flex flex-col overflow-hidden">

          <div class="space-y-4 mb-6">
            <div class="flex justify-between items-center">
              <label class="text-xs font-bold text-gray-500 uppercase tracking-tight">Select Game Type :</label>
              <select id="game-type" onchange="updateExistingBidsType()"
                class="w-1/2 py-2 px-4 bg-white border border-black rounded-full text-center text-sm font-bold focus:ring-2 focus:ring-brand-teal outline-none cursor-pointer">
                <option value="OPEN">OPEN</option>
                <option value="CLOSE">CLOSE</option>
              </select>
            </div>

            <div class="flex justify-between items-center">
              <label class="text-xs font-bold text-gray-500 uppercase tracking-tight">Enter Points :</label>
              <input type="number" id="points-input" value=""
                class="w-1/2 py-2 border border-black rounded-full text-center text-sm font-bold outline-none focus:ring-2 focus:ring-brand-teal">
            </div>
          </div>

          <div id="digit-grid" class="grid grid-cols-5 gap-2 mb-6">
          </div>

          <div style="height: 38vh; overflow-y: auto; padding-bottom: 3.5rem;">
            <div
              class="grid grid-cols-4 text-center text-[10px] font-black text-gray-400 uppercase tracking-widest pb-3 border-b-2 border-gray-300 ">
              <span>Digit</span>
              <span>Points</span>
              <span>Game type</span>
              <span></span>
            </div>

            <div class="flex-1 overflow-y-auto no-scrollbar py-2">
              <div id="bid-list" class="space-y-2"></div>
            </div>
          </div>
        </main>

        <footer class="bg-white p-5 border-t border-gray-200 flex justify-between items-center fixed w-[100%] bottom-0">
          <div class="flex space-x-8">
            <div class="flex flex-col items-center">
              <span class="text-[10px] text-gray-400 font-bold uppercase">Bid</span>
              <b id="total-bid" class="text-xl text-gray-800">0</b>
            </div>
            <div class="flex flex-col items-center">
              <span class="text-[10px] text-gray-400 font-bold uppercase">Total</span>
              <b id="total-points" class="text-xl text-gray-800">0</b>
            </div>
          </div>
          <button onclick="submitData()"
            class="bg-brand-teal text-white px-12 py-3 rounded-xl font-bold shadow-lg text-sm md:text-base active:scale-95 transition-all">
            SUBMIT
          </button>
        </footer>
      </div>
    </div>
  </main>


  <!-- ===================================== Triple Panna ================================ -->

  <main id="triplePanna" data-game-id="<%= game._id %>" data-game-name="<%= game.gameName %>"
    data-open-time="<%= todaySchedule ? todaySchedule.openTime : '' %>"
    data-close-time="<%= todaySchedule ? todaySchedule.closeTime : '' %>"
    class="bg-white min-h-screen flex flex-col hidden fixed top-0 w-full">
    <!-- HEADER -->

    <header class="bg-teal-800 text-white px-4 py-5 rounded-b-3xl">
      <div class="flex items-center gap-3">
        <i id="closeTriplePanna" class="fa-solid fa-arrow-left text-xl cursor-pointer"></i>
        <h1 class="text-lg font-semibold">Triple Panna - <%= game.gameName %>
        </h1>
      </div>
    </header>
    <div class="bg-gray-100 min-h-screen flex justify-center items-start md:items-center">

      <div
        class="w-full max-w-5xl bg-white shadow-xl flex flex-col min-h-screen md:min-h-0 md:rounded-2xl overflow-hidden">

        <div class="p-4 md:p-6 border-b">

          <div class="flex justify-between items-center relative">
            <span class="text-gray-600 font-medium">Select Game Type</span>

            <button id="tp-select-trigger" onclick="tpToggleMenu()"
              class="border-2 border-[#005c4b] rounded-full px-10 py-1 font-bold text-xs uppercase tracking-widest text-[#005c4b] hover:bg-[#005c4b] hover:text-white transition-all">
              OPEN
            </button>

            <div id="tp-dropdown-box"
              class="tp-hidden absolute right-0 top-10 w-40 bg-white border-2 border-[#005c4b] rounded-lg shadow-xl z-50">
              <button onclick="tpSetGameType('OPEN')"
                class="w-full text-left px-4 py-2 font-bold hover:bg-teal-50 border-b border-gray-100 text-[#005c4b]">OPEN</button>
              <button onclick="tpSetGameType('CLOSE')"
                class="w-full text-left px-4 py-2 font-bold hover:bg-teal-50 text-[#005c4b]">CLOSE</button>
            </div>
          </div>
        </div>

        <div class="p-4 md:p-8 bg-gray-50 flex-grow">
          <div id="tp-main-grid" class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-4">
          </div>

          <div class="flex justify-center mt-10">
            <div class="flex border-2 border-[#005c4b] rounded-lg overflow-hidden w-full max-w-xs shadow-sm bg-white">
              <div class="bg-[#005c4b] text-white px-5 py-2 font-bold flex items-center">
                Total
              </div>
              <input type="text" id="tp-final-total" value="0" readonly
                class="w-full p-2 text-center font-bold text-[#005c4b] outline-none text-xl">
            </div>
          </div>
        </div>

        <div class="p-4 md:p-6 bg-white border-t relative bottom-[5rem]">
          <button onclick="tpSubmitFinalData()"
            class="w-full md:max-w-xs mx-auto block bg-[#005c4b] text-white py-4 rounded-full text-lg font-bold shadow-lg hover:brightness-110 active:scale-95 transition-all">
            SUBMIT
          </button>
        </div>
      </div>
    </div>

  </main>


  <!-- ===================================== Odd Even ================================ -->

  <main id="oddEven" data-game-id="<%= game._id %>" data-game-name="<%= game.gameName %>"
    data-open-time="<%= todaySchedule ? todaySchedule.openTime : '' %>"
    data-close-time="<%= todaySchedule ? todaySchedule.closeTime : '' %>"
    class="bg-white min-h-screen flex flex-col hidden fixed top-0 w-full">
    <!-- HEADER -->

    <header class="bg-teal-800 text-white px-4 py-5 rounded-b-3xl">
      <div class="flex items-center gap-3">
        <i id="closeOddEven" class="fa-solid fa-arrow-left text-xl cursor-pointer"></i>
        <h1 class="text-lg font-semibold">Odd Even - <%= game.gameName %>
        </h1>
      </div>
    </header>

    <div class="bg-slate-50 font-sans text-slate-900 min-h-screen">


      <main class="mx-auto px-4 py-6 pb-36 w-[100%]">
        <div style="display: flex; flex-direction: column; justify-content: space-around; height: 75vh;">
          <div class="config-section space-y-5">
            <div class="control-row flex flex-col sm:flex-row sm:items-center justify-between gap-3">
              <div class="dropdown-wrapper relative w-full">
                <select id="selectGameTiming"
                  class="form-select w-full appearance-none border border-slate-300 rounded-full py-2.5 px-6 bg-white focus:ring-2 focus:ring-[#005c4b] outline-none cursor-pointer text-center font-semibold">
                  <option value="OPEN">OPEN</option>
                  <option value="CLOSE">CLOSE</option>
                </select>
                <i
                  class="fas fa-chevron-down absolute right-5 top-1/2 -translate-y-1/2 text-slate-400 pointer-events-none text-xs"></i>
              </div>
            </div>

            <div
              class="pattern-card flex justify-center gap-10 py-5 bg-white rounded-2xl shadow-sm border border-slate-200">
              <label class="pattern-option flex items-center gap-3 cursor-pointer group">
                <div class="radio-wrapper relative">
                  <input type="radio" name="digitPattern" id="radioPatternOdd" class="hidden">
                  <span
                    class="radio-indicator w-6 h-6 border-2 border-slate-300 rounded-full block relative transition-all group-hover:border-[#005c4b]"></span>
                </div>
                <span class="pattern-label font-bold text-lg">Odd</span>
              </label>
              <label class="pattern-option flex items-center gap-3 cursor-pointer group">
                <div class="radio-wrapper relative">
                  <input type="radio" name="digitPattern" id="radioPatternEven" class="hidden">
                  <span
                    class="radio-indicator w-6 h-6 border-2 border-slate-300 rounded-full block relative transition-all group-hover:border-[#005c4b]"></span>
                </div>
                <span class="pattern-label font-bold text-lg">Even</span>
              </label>
            </div>

            <div class="flex sm:flex-row sm:items-center justify-between gap-3"
              style="direction: row; justify-content: space-around;">
              <label class="field-label font-semibold text-slate-600 flex justify-center items-center">Enter Points
                :</label>
              <input type="number" id="inputPointsAmount"
                class="form-input w-full sm:w-2/3 border border-slate-300 rounded-full py-3 px-4 bg-white text-center text-xl font-bold focus:ring-2 focus:ring-[#005c4b] outline-none shadow-inner w-[12rem]">
            </div>

            <button id="btnTriggerBid"
              class="action-btn w-full bg-[#005c4b] text-white font-bold py-4 rounded-xl hover:bg-[#004a3c] transition-all active:scale-[0.98] shadow-lg uppercase tracking-widest text-sm">
              ADD BID
            </button>
          </div>
          <div style="height: 38vh; overflow-y: auto; padding-bottom: 3.5rem;">
            <div
              class="data-table-container mt-8 bg-white rounded-2xl overflow-hidden border border-slate-200 shadow-sm">
              <div
                class="table-head grid grid-cols-4 bg-slate-50 py-4 px-4 border-b text-center text-[10px] font-bold text-slate-400 uppercase tracking-widest">
                <span>Digit</span>
                <span>Amount</span>
                <span>Type</span>
                <span>Action</span>
              </div>

              <div id="bidDisplayArea" class="table-body divide-y divide-slate-100">
                <div class="empty-state p-10 text-center text-slate-400 italic text-sm">Waiting for selection...</div>
              </div>
            </div>
          </div>
        </div>
      </main>

      <footer
        class="status-bar fixed bottom-0 left-0 right-0 bg-white border-t z-30 shadow-[0_-10px_15px_-3px_rgba(0,0,0,0.05)]">
        <div class="max-w-4xl mx-auto px-6 py-4 flex items-center justify-between">
          <div class="stats-group flex gap-8">
            <div class="stat-item text-left">
              <p class="stat-label text-[10px] uppercase text-slate-400 font-bold mb-0.5">Bid Count</p>
              <p id="displayTotalCount" class="stat-value text-2xl font-black text-[#005c4b]">0</p>
            </div>
            <div class="stat-item text-left">
              <p class="stat-label text-[10px] uppercase text-slate-400 font-bold mb-0.5">Total Points</p>
              <p id="displayTotalPoints" class="stat-value text-2xl font-black text-[#005c4b]">0</p>
            </div>
          </div>
          <button
            class="submit-action-btn bg-[#005c4b] text-white px-8 sm:px-12 py-3.5 rounded-xl font-bold shadow-md hover:bg-[#004a3c] transition-all active:shadow-none">
            SUBMIT
          </button>
        </div>
      </footer>
    </div>

  </main>


  <!-- ===================================== Half Sangam ================================ -->
<main id="halfSugam"
  data-game-id="<%= game._id %>"
  data-game-name="<%= game.gameName %>"
  data-open-time="<%= todaySchedule ? todaySchedule.openTime : '' %>"
  data-close-time="<%= todaySchedule ? todaySchedule.closeTime : '' %>"
  class="bg-white min-h-screen flex flex-col hidden fixed top-0 w-full">

  <!-- HEADER -->
  <header class="bg-teal-800 text-white px-4 py-5 rounded-b-3xl">
    <div class="flex items-center gap-3">
      <i id="closeHalfSugam" class="fa-solid fa-arrow-left text-xl cursor-pointer"></i>
      <h1 class="text-lg font-semibold">Half Sangam - <%= game.gameName %></h1>
    </div>
  </header>

  <!-- FORM -->
  <div class="px-5 pt-6 space-y-6 flex-1">

    <!-- OPEN DIGIT -->
    <div class="flex justify-between items-center">
      <h2 class="font-bold text-lg">Open Panna</h2>
      <input id="openDigit" type="number" maxlength="3"
        placeholder="3 digit"
        class="w-48 px-4 py-3 rounded-xl border shadow">
    </div>

    <!-- CLOSE PANNA -->
    <div class="flex justify-between items-center">
      <h2 class="font-bold text-lg">Close Digit</h2>
      <input id="closePanna" type="number" readonly
        class="w-48 px-4 py-3 rounded-xl border bg-gray-100">
    </div>

    <!-- POINTS -->
    <div class="flex justify-between items-center">
      <h2 class="font-bold text-lg">Points</h2>
      <input id="points" type="number" min="1"
        class="w-48 px-4 py-3 rounded-xl border shadow">
    </div>

    <!-- ADD -->
    <div class="flex justify-end">
      <button id="addBid"
        class="bg-teal-800 text-white px-10 py-3 rounded-xl font-bold">
        Add
      </button>
    </div>

    <!-- TABLE -->
    <div class="overflow-x-auto">
      <table class="w-full border text-center text-sm">
        <thead class="bg-gray-200 font-bold">
          <tr>
            <th>Open Panna</th>
            <th>Close Digit</th>
            <th>Points</th>
            <th>Delete</th>
          </tr>
        </thead>
        <tbody id="halfsangambetTable"></tbody>
      </table>
    </div>

  </div>

  <!-- SUBMIT -->
  <button id="HalfSangamSubmit"
    class="bg-green-600 text-white py-4 text-xl font-bold">
    Submit
  </button>

  <!-- BOTTOM -->
  <div class="border-t py-3 flex justify-around font-bold">
    <div>Bids <span id="halfSangamBids">0</span></div>
    <div>Amount ₹<span id="halfSangamAmount">0</span></div>
  </div>

</main>


  <!-- ===================================== Full Sugam ================================ -->

  <main id="fullSugam"
  data-game-id="<%= game._id %>"
  data-game-name="<%= game.gameName %>"
  data-open-time="<%= todaySchedule ? todaySchedule.openTime : '' %>"
  data-close-time="<%= todaySchedule ? todaySchedule.closeTime : '' %>"
  class="bg-white min-h-screen flex flex-col hidden fixed top-0 w-full">

  <!-- HEADER -->
  <header class="bg-teal-800 text-white px-4 py-5 rounded-b-3xl">
    <div class="flex items-center gap-3">
      <i id="closeFullSugam" class="fa-solid fa-arrow-left text-xl cursor-pointer"></i>
      <h1 class="text-lg font-semibold">Full Sangam - <%= game.gameName %></h1>
    </div>
  </header>

  <!-- FORM -->
  <div class="px-5 pt-6 space-y-6 flex-1">

    <!-- OPEN PANNA -->
    <div class="flex justify-between items-center">
      <h2 class="font-bold text-lg">Open Panna</h2>
      <input id="openPannaFullSanagam" type="number" maxlength="3"
        placeholder="3 digit" 
        class="w-48 px-4 py-3 rounded-xl border shadow">
    </div>

    <!-- CLOSE PANNA -->
    <div class="flex justify-between items-center">
      <h2 class="font-bold text-lg">Close Panna</h2>
      <input id="closePannaFullSangam" type="number" readonly
        class="w-48 px-4 py-3 rounded-xl border bg-gray-100">
    </div>

    <!-- POINTS -->
    <div class="flex justify-between items-center">
      <h2 class="font-bold text-lg">Points</h2>
      <input id="fullsangampoints" type="number" min="1"
        class="w-48 px-4 py-3 rounded-xl border shadow">
    </div>

    <!-- ADD -->
    <div class="flex justify-end">
      <button id="fullsangamaddBid"
        class="bg-teal-800 text-white px-10 py-3 rounded-xl font-bold">
        Add
      </button>
    </div>

    <!-- TABLE -->
    <div class="overflow-x-auto">
      <table class="w-full border text-center text-sm">
        <thead class="bg-gray-200 font-bold">
          <tr>
            <th>Open Panna</th>
            <th>Close Panna</th>
            <th>Points</th>
            <th>Delete</th>
          </tr>
        </thead>
        <tbody id="fullsangambetTable"></tbody>
      </table>
    </div>

  </div>

  <!-- SUBMIT -->
  <button id="fullSangamSubmit"
    class="bg-green-600 text-white py-4 text-xl font-bold">
    Submit
  </button>

  <!-- BOTTOM -->
  <div class="border-t py-3 flex justify-around font-bold">
    <div>Bids <span id="fullSangamBids">0</span></div>
    <div>Amount ₹<span id="fullSangamAmount">0</span></div>
  </div>

</main>


  <!-- ===================================== SP Motor ================================ -->
  <main id="sPMotor " data-game-id="<%= game._id %>" data-game-name="<%= game.gameName %>"
    data-open-time="<%= todaySchedule ? todaySchedule.openTime : '' %>"
    data-close-time="<%= todaySchedule ? todaySchedule.closeTime : '' %>"
    class="bg-white min-h-screen flex flex-col hidden fixed top-0 w-full">
    <!-- HEADER -->

    <header class="bg-teal-800 text-white px-4 py-5 rounded-b-3xl">
      <div class="flex items-center gap-3">
        <i id="closeSPMotor " class="fa-solid fa-arrow-left text-xl cursor-pointer"></i>
        <h1 class="text-lg font-semibold">SP Motor - <%= game.gameName %>
        </h1>
      </div>
    </header>

    <!-- FORM -->
    <div class="px-5 pt-8 space-y-8 flex-1">
      <!-- CHOOSE SESSION -->
      <div class="flex justify-between items-center">
        <h2 class="text-xl font-bold text-gray-900">Choose<br />Session</h2>

        <div class="flex space-x-6">
          <label class="flex items-center space-x-2">
            <input type="radio" name="spmotor_session" value="OPEN" checked class="accent-teal-700 w-5 h-5" />
            <span class="font-semibold text-lg">Open</span>
          </label>

          <label class="flex items-center space-x-2">
            <input type="radio" name="spmotor_session" value="CLOSE" class="accent-gray-400 w-5 h-5" />
            <span class="font-semibold text-lg text-gray-600">Close</span>
          </label>
        </div>
      </div>

    <!-- MAIN DIGIT -->
    <div class="flex justify-between items-center">
      <h2 class="text-xl font-bold">Main Digit</h2>
      <input
         id="spmotor_opendigit"
        type="text"
        inputmode="numeric"
        placeholder="0-9"
        class="w-40 px-4 py-3 border rounded-xl font-bold"
      />
    </div>

      <!-- POINTS -->
      <div class="flex justify-between items-center">
        <h2 class="text-xl font-bold text-gray-900">Points</h2>

        <input type="number" id="spmotor_points" placeholder="Enter Point"
          class="w-52 px-4 py-3 rounded-xl shadow-md border border-gray-200 focus:outline-none focus:ring-2 focus:ring-teal-700 font-semibold" />
      </div>

      <!-- ADD BUTTON -->
      <div class="flex justify-end pt-6">
        <button id="spmotor_addbtn"
          class="bg-teal-800 text-white text-xl font-bold px-20 py-4 rounded-2xl shadow-lg active:scale-95 transition">
          Add
        </button>
      </div>

       <!-- TABLE -->
    <table class="w-full text-center border mt-4">
      <thead class="bg-gray-100 font-bold">
        <tr>
          <td>Digit</td>
          <td>Amount</td>
          <td>Session</td>
          <td>Action</td>
        </tr>
      </thead>
      <tbody id="spTable"></tbody>
    </table>

      <!-- SUBMIT BUTTON -->
      <div class="flex justify-center pt-8">
        <button id="spmotor_submitbtn" type="submit" class="bg-gradient-to-r from-purple-700 to-indigo-700 text-white
               text-xl font-bold px-24 py-4 rounded-2xl shadow-xl
               active:scale-95 transition-all duration-150">
          Submit
        </button>
      </div>
    </div>

    <!-- BOTTOM BAR -->
    <div class="border-t-2 border-teal-800 bg-white py-4">
      <div class="flex justify-around text-center font-semibold text-lg">
        <div>
          <div>Bids</div>
          <div class="spmotor_totalbids text-gray-600">0</div>
        </div>
        <div>
          <div>Amount</div>
          <div class="spmotor_totalamount text-gray-600">0</div>
        </div>
      </div>
    </div>
  </main>


  <!-- ===================================== DP Motor ================================ -->

  <main id="dPMotor "
  data-game-id="<%= game._id %>"
  data-game-name="<%= game.gameName %>"
  data-open-time="<%= todaySchedule ? todaySchedule.openTime : '' %>"
  class="bg-white min-h-screen flex flex-col hidden fixed top-0 w-full">

  <!-- HEADER -->
  <header class="bg-teal-800 text-white px-4 py-5 rounded-b-3xl">
    <div class="flex items-center gap-3">
      <i id="closeDPMotor " class="fa-solid fa-arrow-left text-xl cursor-pointer"></i>
      <h1 class="text-lg font-semibold">DP Motor - <%= game.gameName %></h1>
    </div>
  </header>

  <div class="px-5 pt-8 space-y-6 flex-1">

    <!-- SESSION -->
    <div class="flex justify-between items-center">
      <h2 class="text-xl font-bold">Choose Session</h2>
      <div class="flex gap-6">
        <label><input type="radio" name="dpmotor_session" value="OPEN" checked> Open</label>
        <label><input type="radio" name="dpmotor_session" value="CLOSE"> Close</label>
      </div>
    </div>

    <!-- MAIN DIGIT -->
    <div class="flex justify-between items-center">
      <h2 class="text-xl font-bold">Main Digit</h2>
      <input
        id="dpmotor_opendigit"
        type="text"
        inputmode="numeric"
        placeholder="0-9"
        class="w-40 px-4 py-3 border rounded-xl font-bold"
      />
    </div>

    <!-- POINTS -->
    <div class="flex justify-between items-center">
      <h2 class="text-xl font-bold">Points</h2>
      <input id="dpmotor_points" type="number"
        class="w-40 px-4 py-3 border rounded-xl font-bold" />
    </div>

    <!-- ADD -->
    <div class="flex justify-end">
      <button id="dpmotor_addbtn"
        class="bg-teal-800 text-white px-14 py-3 rounded-xl font-bold">
        Add
      </button>
    </div>

    <!-- TABLE -->
    <table class="w-full text-center border mt-4">
      <thead class="bg-gray-100 font-bold">
        <tr>
          <td>Digit</td>
          <td>Amount</td>
          <td>Session</td>
          <td>Action</td>
        </tr>
      </thead>
      <tbody id="dpTable"></tbody>
    </table>

    <!-- SUBMIT -->
    <div class="flex justify-center pt-6">
      <button id="dpmotor_submitbtn"
        class="bg-teal-800 text-white px-24 py-4 rounded-xl font-bold">
        Submit
      </button>
    </div>
  </div>

  <!-- BOTTOM -->
  <div class="border-t py-4">
    <div class="flex justify-around font-bold">
      <div>Bids <div class="dpmotor_totalbids">0</div></div>
      <div>Amount <div class="dpmotor_totalamount">0</div></div>
    </div>
  </div>
</main>

  <!-- ========================= SP DP TP ========================= -->
  <main id="sPDPTP " data-game-id="<%= game._id %>" data-game-name="<%= game.gameName %>"
    data-open-time="<%= todaySchedule ? todaySchedule.openTime : '' %>"
    data-close-time="<%= todaySchedule ? todaySchedule.closeTime : '' %>"
    class="bg-white min-h-screen flex flex-col fixed top-0 w-full hidden">

    <!-- HEADER -->
    <header class="bg-teal-800 text-white px-4 py-5 rounded-b-3xl">
      <div class="flex items-center gap-3">
        <i id="closeSPDPTP" class="fa-solid fa-arrow-left text-xl cursor-pointer"></i>
        <h1 class="text-lg font-semibold">
          SP DP TP - <%= game.gameName %>
        </h1>
      </div>
    </header>

    <!-- FORM + TABLE (SCROLLABLE) -->
    <div class="flex-1 overflow-y-auto p-6 space-y-8">

      <!-- Choose Session -->
      <div class="flex items-center justify-between">
        <span class="font-semibold text-lg">Choose Session</span>
        <div class="flex gap-6">
          <label class="flex items-center gap-2 cursor-pointer">
            <input type="radio" name="SPDPTP_session" checked class="accent-teal-700 w-5 h-5" />
            <span class="font-medium">Open</span>
          </label>
          <label class="flex items-center gap-2 cursor-pointer">
            <input type="radio" name="SPDPTP_session" class="accent-teal-700 w-5 h-5" />
            <span class="font-medium">Close</span>
          </label>
        </div>
      </div>

      <!-- Choose SP DP TP -->
      <div class="flex items-center justify-between">
        <span class="font-semibold text-lg">Choose SP DP TP</span>
        <div class="flex gap-6">
          <label class="flex items-center gap-2">
            <input type="checkbox" id="SP" class="w-5 h-5 accent-teal-700" />
            <span class="font-medium">SP</span>
          </label>
          <label class="flex items-center gap-2">
            <input type="checkbox" id="DP" class="w-5 h-5 accent-teal-700" />
            <span class="font-medium">DP</span>
          </label>
          <label class="flex items-center gap-2">
            <input type="checkbox" id="TP" class="w-5 h-5 accent-teal-700" />
            <span class="font-medium">TP</span>
          </label>
        </div>
      </div>

      <!-- Open Digit -->
      <div class="flex items-center justify-between">
        <span class="font-semibold text-lg">Main Digit</span>
        <input type="number" id="spdptpopen_Digit" placeholder="0 - 9" maxlength="1"
          oninput="this.value = this.value.slice(0,1)" class="w-48 px-4 py-3 rounded-xl shadow-md border
  focus:outline-none focus:ring-2 focus:ring-teal-700" />

      </div>

      <!-- Points -->
      <div class="flex items-center justify-between">
        <span class="font-semibold text-lg">Points</span>
        <input type="number" id="spdptp_Points" placeholder="Enter Points" class="w-48 px-4 py-3 rounded-xl shadow-md border
        focus:outline-none focus:ring-2 focus:ring-teal-700" />
      </div>

      <!-- ADD BUTTON -->
      <button id="spdptpAddBtn" class="w-full bg-teal-800 text-white py-4 rounded-2xl
      font-semibold text-lg shadow-lg active:scale-95 transition">
        Add
      </button>

      <!-- TABLE -->
      <table class="w-full text-center mt-6 border">
        <thead class="bg-gray-100">
          <tr class="border-b font-bold">
            <td>Digit</td>
            <td>Choose</td>
            <td>Amount</td>
            <td>Session</td>
            <td>Action</td>
          </tr>
        </thead>
        <tbody id="spdptpTable"></tbody>
      </table>

    </div>

    <!-- SUBMIT (STICKY) -->
    <div class="sticky bottom-0 bg-white border-t py-4 flex justify-center">
      <button id="spdptp_submitbtn" class="bg-teal-800 text-white text-xl font-bold
      px-24 py-4 rounded-2xl shadow-lg active:scale-95 transition">
        Submit
      </button>
    </div>

    <!-- FOOTER -->
    <div class="border-t flex justify-around py-4 text-center bg-white">
      <div>
        <p class="font-semibold">Bids</p>
        <p class="spdptp_totalbids text-lg">0</p>
      </div>
      <div>
        <p class="font-semibold">Amount</p>
        <p class="spdptp_totalbids text-lg">0</p>
      </div>
    </div>

  </main>



  <!-- ===================================== Red Bracket ================================ -->

  <main id="redBracket" data-game-id="<%= game._id %>" data-game-name="<%= game.gameName %>"
    data-open-time="<%= todaySchedule ? todaySchedule.openTime : '' %>"
    data-close-time="<%= todaySchedule ? todaySchedule.closeTime : '' %>"
    class="bg-white min-h-screen flex flex-col hidden fixed top-0 w-full">
    <!-- HEADER -->
    <header class="bg-teal-800 text-white px-4 py-5 rounded-b-3xl">
      <div class="flex items-center gap-3">
        <i id="closeRedBracket" class="fa-solid fa-arrow-left text-xl cursor-pointer"></i>
        <h1 class="text-lg font-semibold"> Red Bracket - <%= game.gameName %>
        </h1>
      </div>
    </header>
    <div class="bg-slate-50 font-sans text-slate-900 min-h-screen ">

      <main class="max-w-xl mx-auto px-4 py-8 pb-36 overflow-y-auto h-screen">

        <div
          class="flex justify-center items-center gap-8 sm:gap-16 py-6 mb-6 bg-white rounded-2xl shadow-sm border border-slate-200">
          <label class="flex items-center gap-3 cursor-pointer group">
            <div class="relative flex items-center justify-center">
              <input type="radio" name="bracketMode" id="modeHalf" class="hidden" checked>
              <span
                class="radio-indicator w-6 h-6 border-2 border-slate-300 rounded-full block relative transition-all group-hover:border-[#005c4b]"></span>
            </div>
            <span class="font-bold text-sm sm:text-base">Half Bracket</span>
          </label>
          <label class="flex items-center gap-3 cursor-pointer group">
            <div class="relative flex items-center justify-center">
              <input type="radio" name="bracketMode" id="modeFull" class="hidden">
              <span
                class="radio-indicator w-6 h-6 border-2 border-slate-300 rounded-full block relative transition-all group-hover:border-[#005c4b]"></span>
            </div>
            <span class="font-bold text-sm sm:text-base">Full Bracket</span>
          </label>
        </div>

        <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-4 mb-6">
          <label class="font-semibold text-slate-600 text-sm sm:text-base">Enter Points :</label>
          <input type="number" id="inputPoints" value="10"
            class="w-full sm:w-2/3 border border-slate-300 rounded-full py-2.5 px-4 bg-white text-center text-lg font-bold focus:ring-2 focus:ring-[#005c4b] outline-none shadow-inner">
        </div>

        <button id="btnAddBid"
          class="w-full bg-[#005c4b] text-white font-bold py-4 rounded-xl hover:bg-[#004a3c] transition-all active:scale-[0.98] shadow-lg uppercase tracking-widest text-sm mb-8">
          ADD BID
        </button>

        <div class="bg-white rounded-2xl overflow-hidden border border-slate-200 shadow-sm">
          <div
            class="grid grid-cols-4 bg-slate-50 py-4 px-2 border-b text-center text-[10px] font-bold text-slate-400 uppercase tracking-widest">
            <span class="flex items-center justify-center">Digit</span>
            <span class="flex items-center justify-center">Amount</span>
            <span class="flex items-center justify-center">Game type</span>
            <span class="flex items-center justify-center">Action</span>
          </div>

          <div id="bidTableBody" class="divide-y divide-slate-100">
            <div class="p-10 text-center text-slate-400 italic text-sm">No active bids</div>
          </div>
        </div>
      </main>

      <footer class="fixed bottom-0 left-0 right-0 bg-white border-t z-30 shadow-[0_-10px_15px_-3px_rgba(0,0,0,0.05)]">
        <div class="max-w-4xl mx-auto px-6 py-4 flex items-center justify-between">
          <div class="flex gap-8 sm:gap-12 items-center">
            <div class="flex flex-col items-center">
              <p class="text-[10px] uppercase text-slate-400 font-bold mb-1">Bid</p>
              <p id="totalBidCount" class="text-xl sm:text-2xl font-black text-[#005c4b] leading-none">0</p>
            </div>
            <div class="flex flex-col items-center">
              <p class="text-[10px] uppercase text-slate-400 font-bold mb-1">Total</p>
              <p id="totalPointsSum" class="text-xl sm:text-2xl font-black text-[#005c4b] leading-none">0</p>
            </div>
          </div>
          <button id="redBracketSubmitBtnn"
            class="bg-[#005c4b] text-white px-8 sm:px-14 py-3.5 rounded-xl font-bold shadow-md hover:bg-[#004a3c] transition-all active:shadow-none uppercase text-sm">
            Submit
          </button>
        </div>
      </footer>
    </div>
  </main>


  <!-- ============== All Script ================ -->
  <script src="/js/userMainGame.js"></script>
  <script src="/js/singleDigit.js"></script>
  <script src="/js/singleBulkDigit.js"></script>
  <script src="/js/jodiDigit.js"></script>
  <script src="/js/jodiDigitBulk.js"></script>
  <script src="/js/singlePanna.js"></script>
  <script src="/js/doublePanna.js"></script>
  <script src="/js/triplePanna.js"></script>
  <script src="/js/singlePannaBulk.js"></script>
  <script src="/js/doublePannaBulk.js" defer></script>
  <script src="/js/oddEven.js"></script>
  <script src="/js/HalfSangam.js"></script>
  <script src="/js/redBracket.js"></script>
  <script src="/js/FullSangam.js"></script>
  <script src="/js/spMotor.js"></script>
  <script src="/js/dpMotor.js"></script>
  <script src="/js/spdptp.js"></script>

</body>

</html>